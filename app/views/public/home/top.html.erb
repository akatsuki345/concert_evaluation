<div class="container px-5 px-sm-0">
 <div class="row">
  <div class="col-sm-2 mt-5">
   <%= render 'public/tmp/category_list', categories: @categories %>
  </div>

  <div class="col-md-8 offset-md-1 mt-5">
    <div class="row">
      <div class="position-relative" style="left: 120px;">
        <%= image_tag 'cake-1850011_960_720', size:"600x350", style: "margin-bottom: -20px;" %>
        <div class="card-img-overlay d-flex justify-content-center align-items-center">
          <h3 class="card-title text-light text-center">
            ようこそ、concert_evaluationへ!<br>
            このサイトは、コンサートのECサイトになります。<br>
            会員でない方も閲覧はできますが、<br>
            チケットの購入には会員登録が必要になります。
          </h3>
        </div>
      </div>
    <div class="container">
    <div class="row">
      <div class="col-md-10 offset-md-1 mt-5">
        <h3 style="margin-left: -150px;">新着情報</h3>
        <table class="table table-borderless float-right">
          <tr>
            <% @concerts.each do |concert| %>
              <td>
                <%= link_to(image_tag(concert.get_image, size: '230x150', class: 'card-image-top'), concert_path(concert.id)) %>
                <%= link_to concert.name, concert_path(concert.id) %></br>
                <%= to_total_currency(concert.add_tax_price) %>
              </td>
            <% end %>
          </tr>
        </table>
      </div>
    </div>
  <div class="text-right">
    <%= link_to "全ての商品を見る＞", concerts_path %>
  </div>
</div>