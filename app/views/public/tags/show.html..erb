<h1>Tags</h1>

<%= link_to 'Tags List', public_tags_path %>
<%#= link_to 'Delete', admin_tag_path(@tag), method: :delete, data: {confirm: '関連づいた記事のタグが空になりますがタグを削除しますか？'} %>

<h2>Tag</h2>
<%# タグ名 %>
<h3><%= @tag.name %></h3>

<%# タグに関連付く記事一覧 %>
<ul>
  <% @tag.concerts.each do |concert| %>
    <li>
      <%= link_to concert.name, public_concert_path(concert) %>
    </li>
  <% end %>
</ul>